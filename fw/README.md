# Firmware Stack

The firmware layer provides a secure boot ROM and UEFI-like runtime services implemented in portable C. The code targets QEMU for emulation and logs over stdout to mimic UART output.
