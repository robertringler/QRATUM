# Traceability Matrix — P0.5

Every published claim terminates in: code → metric → invariant → test
→ formal model → replay receipt → CI gate. A row with any empty cell
is a HARD FAIL (CI gate `P0.5 — traceability hardened orphan check`).

| # | CLAIM                                                          | INVARIANT  | TRACE POINT                       | METRIC                                        | TEST                                             | FORMAL MODEL                            | REPLAY RECEIPT                       | CI GATE                                              |
|---|----------------------------------------------------------------|-----------:|-----------------------------------|-----------------------------------------------|--------------------------------------------------|-----------------------------------------|--------------------------------------|------------------------------------------------------|
| 1 | Live execution replays bit-for-bit                             | INV-J      | `SchedEnter`, `IntentSubmit`      | `replay::diff()` in `replay.rs`               | `determinism::*`                                  | `replay_consistency.tla`                | `ExecutionReceipt::Replay`           | `P0.3 — determinism`                                 |
| 2 | Audit chain is hash-linked Merkle                              | INV-A,B,C  | `AuditEmit`                       | `audit_recovery::merkle_root`                 | `audit_recovery::*`                              | `execution_equivalence.tla`             | `audit::FrameKind`                   | `P0.3 — audit recovery`                              |
| 3 | Arbitration verdict depends only on canonical key              | INV-N      | `IntentSubmit`                    | `arbitration_invariance.tla::verdict_*`       | `adversarial_fold_tests::*`                      | `arbitration_invariance.tla`            | `ExecutionReceipt::Submit`           | `P0.4 — AHTC-K adversarial corpus (INV-N)`           |
| 4 | Scheduler event reduction at 70 % duplication                  | INV-J      | `SchedEnqueue`, `AhtcFold`        | `performance::WorkloadReport.enqueues`        | `performance_truth::*`                            | `execution_equivalence.tla`             | `ExecutionReceipt::Fold`             | `P0.3.2 — AHTC-K reality`                            |
| 5 | Canonical execution reduction (memory)                         | INV-J      | `AhtcFold`                        | `WorkloadReport.memory_bytes`                 | `ahtc_k_reality_constraint_tests::*`             | `canonical_equivalence_boundary.tla`    | `ExecutionReceipt::Fold`             | `P0.3.2 — AHTC-K reality`                            |
| 6 | Semantic-preserving execution reduction                        | INV-J,N    | `AhtcFold`, `AuditEmit`           | `replay_equivalence_bit_for_bit`              | `ahtc_k_tests::*`                                 | `canonical_equivalence_boundary.tla`    | `ReceiptChain::replay`               | `P0.3 — AHTC-K`                                       |
| 7 | No task on two run queues                                      | INV-K      | `SchedEnqueue`                    | `smp_model::no_dual_occupancy`                | `smp_determinism::*`, `cross_core_invariants::*` | `multicore_replay.tla`                  | `RunQueueReceipt`                    | `P0.4 — SMP determinism`, `… cross-core invariants`  |
| 8 | Identical migration log → identical replay hash                | INV-L,P    | `Migration` (via `MigrationLog`)  | `smp_model::migration_replay_hash`            | `migration_replay::*`, `migration_ordering::*`   | `migration_consistency.tla`             | `MigrationReceipt`                   | `P0.4 — migration replay (INV-L)`                    |
| 9 | Composed scheduler hash is core-order independent              | INV-M,O    | `SchedEnqueue`, `Migration`       | `smp_model::composed_state_hash`              | `cross_core_invariants::*`, `live_multicore_hash::*` | `multicore_replay.tla`              | `RunQueueReceipt`                    | `P0.5 — live multicore hash`                         |
|10 | Semantically distinct intents never fold                       | INV-N      | `IntentSubmit`, `AhtcFold`        | `divergence_detector::report_collision`       | `adversarial_fold_tests::*`, `replay_divergence_detection::*` | `canonical_equivalence_boundary.tla` | `ExecutionReceipt::Submit`        | `P0.5 — replay divergence detection`                 |
|11 | Cache locality improves under canonical execution reduction    | INV-J      | `AhtcFold`                        | `hardware_profile::cache_locality_q`          | `cache_behavior::*`, `cache_locality_reduction::*` | `execution_equivalence.tla`           | `RunQueueReceipt`                    | `P0.5 — cache locality reduction`                    |
|12 | Run-to-run arbitration latency is within envelope              | INV-J      | `SchedEnter`, `SchedExit`         | `LatencyEnvelope::within`                     | `latency_stability::*`                           | `replay_consistency.tla`                | `ExecutionReceipt::Submit`           | `P0.4 — latency stability`                           |
|13 | Context-switch cost is host-self-stable                        | INV-J      | `PreemptIrq`                      | `HardwareProfile.ctx_total_ns`                | `context_switch_equivalence::*`                  | `replay_consistency.tla`                | `RunQueueReceipt`                    | `P0.4 — context-switch equivalence`                  |
|14 | No banned headline terminology in published docs               | INV-G      | n/a                                | `performance_truth::validate_text`            | `performance_claim_integrity::*`, `claim_language_enforcement::*` | n/a                          | n/a                                  | `P0.5 — claim language enforcement`                  |
|15 | External one-command reproduction                              | INV-J      | n/a                                | `tools/repro/reproduce.{ps1,sh}` exit 0       | `tools/repro/*` (in `reproducibility.yml`)        | n/a                                     | `ReceiptChain::chain_hash`           | `P0.4 — external reproducibility`                    |
|16 | Lock acquisition graph is acyclic                              | INV-R      | `LockAcquire`, `LockRelease`      | `lock_graph::has_cycle`                       | `lock_graph_tests::*`                            | `multicore_replay.tla`                  | `ContentionEntry`                    | `P0.5 — lock graph`                                   |
|17 | Memory operations satisfy declared ordering                    | INV-S      | `MemoryOp`                        | `memory_order::violations`                    | `memory_order_consistency::*`                    | `multicore_replay.tla`                  | n/a                                  | `P0.5 — memory order`                                 |
|18 | Migration produces chained receipt                             | INV-Q      | `Migration`                       | `migration_receipt::chain_hash`               | `receipt_chain_integrity::*`, `receipt_replay_consistency::*` | `migration_consistency.tla` | `MigrationReceipt`                   | `P0.5 — receipt chain integrity`                     |
|19 | 1000-scenario adversarial corpus is byte-stable                | INV-J      | n/a                                | `corpus_hash`                                 | `corpus_1000_equivalence::*`                     | `canonical_equivalence_boundary.tla`    | `ReceiptChain::chain_hash`           | `P0.5 — corpus 1000 equivalence`                     |
|20 | NUMA migration penalty is monotone in distance + working set   | INV-J      | `Migration`                       | `numa_model::migration_penalty_cycles`        | `migration_cache_penalty::*`                     | n/a                                     | `MigrationReceipt`                   | `P0.5 — migration cache penalty`                     |
|21 | Hardware profile is stable across runs                         | INV-G      | n/a                                | `HardwareProfile.serde_round_trip`            | `hardware_profile_consistency::*`                 | n/a                                     | n/a                                  | `P0.5 — hardware profile consistency`                |

## Orphan check

Run via:

```bash
grep -E '^\| *[0-9]+' spec/traceability_matrix.md \
  | awk -F'|' '{ for (i=2;i<=NF;i++) if ($i ~ /^[[:space:]]*$/) { print "ORPHAN row: "$0; exit 1 } }'
```

The CI gate fails if any data row contains an empty cell.

| INV-T | AP startup produces exactly one startup receipt           | smp/ap_startup.rs            | interrupt_receipt_consistency        | spec/replay_receipt_spec.md            | hardware_replay_consistency.tla     | smp::ap_startup::chain_hash         |
| INV-U | Replay checkpoint restore reproduces identical receipt    | audit_replay_checkpoint.rs   | replay_checkpoint_consistency        | spec/replay_receipt_spec.md            | replay_checkpoint_recovery.tla      | replay_checkpoint::Checkpoint       |
| INV-V | Work stealing replay-stable across N cores                | smp/deterministic_work_steal | deterministic_work_steal             | spec/deterministic_multicore_model.md  | deterministic_work_steal.tla        | StealRecord chain                   |
| INV-W | xAPIC and x2APIC paths execution-equivalent               | smp/x2apic.rs                | x2apic_equivalence                   | spec/replay_receipt_spec.md            | hardware_replay_consistency.tla     | canonical_ipi pair                  |
| INV-X | Receipt Merkle roots deterministic                        | runtime_model/receipt_merkle | receipt_merkle_integrity             | spec/cryptographic_anchor_spec.md      | receipt_merkle_consistency.tla      | merkle root over leaves             |
| INV-Y | Divergence envelope rejects schedule mutation             | runtime_model/divergence_env | divergence_envelope_tests            | spec/divergence_policy.md              | divergence_envelope.tla             | Envelope tuple                      |
| INV-Z | Cryptographic receipt anchors append-only                 | audit_receipt_anchor.rs      | receipt_anchor_integrity             | spec/cryptographic_anchor_spec.md      | receipt_merkle_consistency.tla      | Anchor.seq monotone, prev_link      |
