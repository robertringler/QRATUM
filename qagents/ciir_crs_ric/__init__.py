"""CIIR-CRS-RIC strict five-module constraint-governed execution package.

Modules:
    failures  — Type I-IV failure taxonomy
    ciir      — Constraint-Induced Interface Realism (S, C, phi, Inv, Omega)
    crs       — Constrained Reality Substrate (A, T, T_C, A_C(s))
    ric       — Reality Interface Controller (pi, rho); state accessed only via Omega
    executor  — 9-step closed-loop execution + TraceEntry schema
    demo      — runnable demonstration with Cases A/B/C/D and analysis report
"""
