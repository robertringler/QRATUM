# Test Infrastructure

Regression suites cover RTL simulation (Verilator), UVM components, and Python-based runtime checks executed via pytest. Continuous integration runs `make lint`, `make sim`, and `make test` to validate contributions.
